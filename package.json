{
  "name": "bir-compliance-toolkit",
  "version": "0.1.0",
  "description": "Google Apps Script toolkit for BIR compliance document generation",
  "scripts": {
    "lint": "eslint src/**/*.gs src/**/*.ts --ext .gs,.ts,.js",
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "eslint": "^8.57.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=20"
  }
}
